/**
 * @jest-environment node
 */

// eslint-disable-next-line jest/no-disabled-tests -- Suite is intentionally skipped due to documented issues.
describe.skip('NextAuth Callbacks (Node & Shared) - Suite is intentionally skipped due to issues', () => {
  it('this entire suite is skipped and this test should not run', () => {
    // This test will not run
    expect(true).toBe(false); // This would fail if run
  });
});
