export const getUserService = jest.fn();
export const getProfileService = jest.fn();
export const getRawQueryService = jest.fn();

// You can provide default mock implementations here if desired,
// for example, to return a generic mock service instance.
// Or, tests can provide their own implementations via:
// import { getUserService } from '@/lib/server/services';
// (getUserService as jest.Mock).mockResolvedValue(...);
